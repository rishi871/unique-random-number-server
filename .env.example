# .env.example
# This file is an example. Copy it to a new file named .env and fill in your values.
# The .env file will be used for local development and should NOT be committed to Git.

# For local development with the provided docker-compose.yml:
DB_SHARD_0_URL=postgresql+psycopg2://user:password@localhost:5433/numbers_db
DB_SHARD_1_URL=postgresql+psycopg2://user:password@localhost:5434/numbers_db

# --- EXAMPLE FOR A REMOTE DATABASE (e.g., AWS RDS) ---
# DB_SHARD_0_URL=postgresql+psycopg2://<db_user>:<db_password>@<your-rds-endpoint>.rds.amazonaws.com:5432/numbers_db
# DB_SHARD_1_URL=postgresql+psycopg2://<db_user>:<db_password>@<your-other-rds-endpoint>.rds.amazonaws.com:5432/numbers_db